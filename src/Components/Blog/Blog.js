import React from 'react';

const Blog = () => {
    return (
        <>
            <div className="container-fluid py-5">
                <div className="container">
                    <h1>Difference between javascript and nodejs</h1>
                    <p>JavaScript is a simple programming language that runs in any browser JavaScript Engine. Whereas Node JS is an interpreter or running environment for a JavaScript programming language that holds many excesses, </p>
                    <h1>When should you use nodejs and when should you use mongodb</h1>
                    <p>1

                        Nodejs is a Javascript engine that you can write any application. It runs on Javascript code.

                        MongoDB is a database engine. Code within some application or server uses MongoDB to save, query or update data in a database. </p>
                    <h1>What is the purpose of jwt and how does it work</h1>
                    <p>JWT, or JSON Web Token, is an open standard used to share security information between two parties — a client and a server</p>
                </div>
            </div>
        </>
    );
};

export default Blog;